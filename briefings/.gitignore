# build artefacts
node_modules/
package.json
package-lock.json

# Visual-QA renders. render_pptx.py and the PDF checks rasterise pages to PNG
# for inspection; none of them are deliverables. Deliverables here are .pptx,
# .pdf, .html and the scripts that build them.
*.png
*.jpg
*.jpeg

*.pdf.tmp
